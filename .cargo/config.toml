# getrandom 0.3 (pulled in via ulid -> rand) requires an explicit JS backend
# on wasm32-unknown-unknown; without this cfg the wasm build fails to compile.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
